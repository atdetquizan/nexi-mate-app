import { ReactiveDocumentValidateDirective } from './reactive-document-validate.directive';

describe('ReactiveDocumentValidateDirective', () => {
  it('should create an instance', () => {
    const directive = new ReactiveDocumentValidateDirective();
    expect(directive).toBeTruthy();
  });
});
