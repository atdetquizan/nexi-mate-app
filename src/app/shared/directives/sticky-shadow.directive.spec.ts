import { StickyShadowDirective } from './sticky-shadow.directive';

describe('StickyShadowDirective', () => {
  it('should create an instance', () => {
    const directive = new StickyShadowDirective();
    expect(directive).toBeTruthy();
  });
});
